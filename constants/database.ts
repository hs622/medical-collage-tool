
export const DEFAULT_LIMIT = 10;
export const MAX_LIMIT = 100;
export const DEFAULT_SKIP = 0;


// ---------- Roles ---------------------------------------------- 

// ---------- Users ----------------------------------------------

// ---------- Modules --------------------------------------------

export const MODULES_COLUMNS = `id, title, description, slug, has_associated, visibility, created_at, updated_at`;

// ---------- Sessions ------------------------------------------- 